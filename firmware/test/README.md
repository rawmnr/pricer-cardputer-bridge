# Firmware tests

The first scaffold keeps protocol unit tests on the Python side and validates firmware through CI compilation. Add PlatformIO Unity tests when parser behavior is factored away from Arduino dependencies or when hardware-in-the-loop infrastructure is available.
